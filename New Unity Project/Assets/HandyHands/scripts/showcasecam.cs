using UnityEngine;
using System.Collections;

public class showcasecam : MonoBehaviour {
	
	//public Transform rotationPoint = Camera.current.transform;
	
	// Use this for initialization
	void Start () {
        //Debug.Log("Test start here");
	}
	
	// Update is called once per frame
	void Update () {
        //Debug.Log("In the update loop");
		//transform.RotateAround(rotationPoint.position, Vector3.up, 20 * Time.deltaTime);
	}
}
